/**
 * @description
 * Interface para configuração das ações do thf-table.
 *
 * @usedBy ThfTableComponent
 */
export interface ThfTableAction {
    /** Nome da função que deve ser executada. */
    function: string;
    /** Nome exibido na lista de ações da tabela. */
    label: string;
}
