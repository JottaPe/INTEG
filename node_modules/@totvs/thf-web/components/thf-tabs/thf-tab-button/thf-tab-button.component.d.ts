import { AfterViewChecked, EventEmitter, ElementRef } from '@angular/core';
export declare class ThfTabButtonComponent implements AfterViewChecked {
    private el;
    id: string;
    active: boolean;
    label: string;
    width: number;
    small: boolean;
    disabled: boolean;
    hide: boolean;
    clickCallback: EventEmitter<{}>;
    clickTab: EventEmitter<{}>;
    tab: ElementRef;
    onClick(e: any): void;
    constructor(el: ElementRef);
    controlDisabled(): void;
    controlActive(): void;
    ngAfterViewChecked(): void;
}
