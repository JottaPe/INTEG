import { ElementRef, OnInit, ViewContainerRef } from '@angular/core';
import { Router } from '@angular/router';
import { Location } from '@angular/common';
import { ThfMenuBaseComponent, ThfMenuItem } from '@totvs/thf-core/components/thf-menu-base';
/**
 * @extends ThfMenuBaseComponent
 *
 * @description
 *
 * Aparece completo em telas com largura que maior que 1200px, caso contrário o menu é escondido e chamado
 * por meio de um botão.
 *
 * Para o menu funcionar corretamente é necessário importar o ```RouterModule```  e ```Routes``` do módulo principal de
 *  sua aplicação:
 *
 * ````
 * import { RouterModule, Routes } from '@angular/router';
 *
 * ...
 *
 * @NgModule({
 *   imports: [
 *     RouterModule,
 *     Routes,
 *     ...
 *     ThfModule,
 *     ...
 *   ],
 *   declarations: [
 *     AppComponent
 *   ],
 *   providers: [],
 *   bootstrap: [AppComponent]
 * })
 * export class AppModule { }
 * ```
 *
 * Além disso é necessário criar um módulo configurando as rotas da aplicação.
 *
 * ```
 * import { NgModule } from '@angular/core';
 *
 * import { RouterModule, Routes } from '@angular/router';
 *
 * import { HelloWorldComponent } from './hello-world/hello-world.component';
 *
 * const routes: Routes = [
 *   {path: 'hello-world', component: HelloWorldComponent}
 * ];
 *
 * @NgModule({
 *   imports: [RouterModule.forRoot(routes, {useHash: true})],
 *   exports: [RouterModule]
 * })
 * export class AppRoutingModule {}
 * ```
 *
 * @example
 * <example name='thf-menu-labs' title='Totvs Menu Labs'>
 * <file name='sample-thf-menu-labs.component.html'> </file>
 * <file name='sample-thf-menu-labs.component.ts'> </file>
 * </example>
 */
export declare class ThfMenuComponent extends ThfMenuBaseComponent implements OnInit {
    private elementRef;
    private viewRef;
    private location;
    private router;
    menuItemActive: ThfMenuItem;
    linkActive: string;
    menuItemOpened: ThfMenuItem;
    menuMobileOpened: boolean;
    parentRef: any;
    constructor(elementRef: ElementRef, viewRef: ViewContainerRef, location: Location, router: Router);
    ngOnInit(): void;
    validateMenus(): void;
    clickMenuItem(menu: ThfMenuItem): void;
    clickMenuItemGrouped(menu: ThfMenuItem): void;
    clickSubMenuItem(subMenu: ThfMenuItem): void;
    toggleMenuMobile(): void;
    isMenuItemSelected(menu: any): any;
    checkActiveMenuByUrl(urlPath: any): void;
    checkUrl(menu: ThfMenuItem): string;
    setActiveMenuByUrl(urlPath: any): void;
    private executeMenuAction(menu);
    private openExternalLink(url);
    private isExternalLink(url);
}
