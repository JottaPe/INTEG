import { ElementRef } from '@angular/core';
import { ViewContainerRef, AfterContentInit } from '@angular/core';
/**
 * @description
 *
 * Este componente é utilizado em conjunto thf-list.
 */
export declare class ThfListItemComponent implements AfterContentInit {
    private myElement;
    private viewRef;
    /** Parâmetro que recebe um titulo a ser renderizado no item.*/
    title: string;
    /** Índice do for utilizado para controles internos do componente.*/
    index: number;
    /** Lista de ações a ser executadas no item. Ao passar até duas ações é exibido apenas botões,
     * se passar de duas ações é exibido um menu com todas as opções.
     */
    actions: Array<any>;
    detailsContent: any;
    content: any;
    _parentRef: any;
    collapsed: boolean;
    showLink: boolean;
    showContent: boolean;
    constructor(myElement: ElementRef, viewRef: ViewContainerRef);
    ngAfterContentInit(): void;
    showDetails(index: any): void;
    readonly parentRef: any;
}
