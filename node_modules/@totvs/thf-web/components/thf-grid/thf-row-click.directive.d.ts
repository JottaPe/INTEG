import { ElementRef, EventEmitter, Renderer2, OnInit } from '@angular/core';
export declare class ThfRowClickDirective implements OnInit {
    private el;
    private renderer;
    editRow: EventEmitter<number>;
    saveRow: EventEmitter<any>;
    constructor(el: ElementRef, renderer: Renderer2);
    ngOnInit(): void;
}
