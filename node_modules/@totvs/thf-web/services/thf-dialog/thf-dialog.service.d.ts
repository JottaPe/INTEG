import { ThfComponentInjectorService } from './../thf-component-injector/thf-component-injector.service';
import { ThfDialogBaseService } from '@totvs/thf-core/services/thf-dialog-base/thf-dialog-base.service';
import { ThfDialogConfirmOptions, ThfDialogAlertOptions } from '@totvs/thf-core/services/thf-dialog-base/thf-dialog.interface';
import { ThfDialogType } from '@totvs/thf-core/services/thf-dialog-base/thf-dialog.enum';
/**
 * @extends ThfDialogBaseService
 *
 * @example
 *
 * <example name='thf-dialog-labs' title='Totvs Dialog Labs'>
 *  <file name='sample-thf-dialog-labs.component.html'>
 *    <thf-button
 *      name="openDialog"
 *      t-icon="thf-icon-logo-totvs"
 *      t-label="Abrir Diálogo"
 *      t-primary="true"
 *      [t-disabled]="f.invalid"
 *      (t-click)="openDialog()">
 *    </thf-button>
 *
 *    <br><br>
 *    <span *ngIf="choice">
 *      <b>Opção selecionada: </b>{{ choice }}
 *    </span>
 *
 *    <hr>
 *
 *    <form #f="ngForm">
 *      <thf-radio-group
 *        name="dialogOptions"
 *        [(ngModel)]="dialogType"
 *        t-label="Opções de diálogo"
 *        t-required
 *        [t-options]="dialogOptions">
 *      </thf-radio-group>
 *
 *      <thf-input
 *        name="title"
 *        [(ngModel)]="title"
 *        t-clean
 *        t-label="Título"
 *        t-placeholder="Insira o título da caixa diálogo"
 *        t-required>
 *      </thf-input>
 *
 *      <thf-input
 *        name="mensage"
 *        [(ngModel)]="mensage"
 *        t-clean
 *        t-label="Mensagem"
 *        t-placeholder="Insira a mensagem da caixa diálogo"
 *        t-required>
 *      </thf-input>
 *    </form>
 *  </file>
 *  <file name='sample-thf-dialog-labs.component.ts'>
 *    import { Component } from '@angular/core';
 *
 *    import { ThfModule } from '@totvs/thf-web';
 *    import { ThfDialogService } from '@totvs/thf-web/services/thf-dialog/thf-dialog.service';
 *
 *    @Component({
 *      selector: 'sample-thf-dialog-labs',
 *      providers: [ThfDialogService],
 *      templateUrl: './sample-thf-dialog-labs.component.html',
 *    })
 *
 *    export class SampleThfDialogLabsComponent {
 *      title = 'Confirmação';
 *      mensage = 'Você tem certeza que deseja efetuar o pedido número #42?';
 *      dialogType = 'confirm';
 *
 *      choice: string;
 *
 *      dialogOptions: Array<any> = [
 *        { label: 'Alert Dialog', value: 'alert' },
 *        { label: 'Confirm Dialog', value: 'confirm' }
 *      ];
 *
 *      constructor(private thfAlert: ThfDialogService) { }
 *
 *      openDialog() {
 *        this.choice = '';
 *        if (this.dialogType === 'alert') {
 *          this.alertDialog();
 *        } else if (this.dialogType === 'confirm') {
 *          this.confirmDialog();
 *        }
 *      }
 *
 *      alertDialog() {
 *        this.thfAlert.alert({
 *          title: this.title,
 *          message: this.mensage,
 *          ok: () => this.yourChoice('OK')
 *        });
 *      }
 *
 *      confirmDialog() {
 *        this.thfAlert.confirm({
 *          title: this.title,
 *          message: this.mensage,
 *          confirm: () => this.yourChoice('CONFIRMAR'),
 *          cancel: () => this.yourChoice('CANCELAR')
 *        });
 *      }
 *
 *      yourChoice(choice) {
 *        this.choice = choice;
 *      }
 *    }
 *  </file>
 * </example>
 */
export declare class ThfDialogService extends ThfDialogBaseService {
    private thfComponentInjector;
    constructor(thfComponentInjector: ThfComponentInjectorService);
    openDialog(dialogType: ThfDialogType, dialogOptions: ThfDialogAlertOptions | ThfDialogConfirmOptions): void;
}
