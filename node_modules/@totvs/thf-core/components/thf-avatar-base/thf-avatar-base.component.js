import { Input } from '@angular/core';
/**
 * \@description
 * O componente thf-avatar é um container para imagens em miniatura, possui um formato redondo e cinco opções de
 * tamanho, pode ser utilizado por exemplo para mostrar a foto de perfil de um usuário.
 *
 */
var ThfAvatarBaseComponent = (function () {
    function ThfAvatarBaseComponent() {
        /**
         * Tamanho do componente que aceita os valores: ```xs``` (24x24) ,```sm``` (32x32),```md```(64x64),```lg```(96x96)
         * ou ```xl```(144x144), O valor padrão é ```md```.
         */
        this.size = 'md';
    }
    Object.defineProperty(ThfAvatarBaseComponent.prototype, "setSize", {
        /**
         * @param {?} value
         * @return {?}
         */
        set: function (value) {
            var /** @type {?} */ sizes = ['xs', 'sm', 'md', 'lg', 'xl'];
            this.size = sizes.includes(value) ? value : 'md';
        },
        enumerable: true,
        configurable: true
    });
    ThfAvatarBaseComponent.propDecorators = {
        'src': [{ type: Input, args: ['t-src',] },],
        'setSize': [{ type: Input, args: ['t-size',] },],
    };
    return ThfAvatarBaseComponent;
}());
export { ThfAvatarBaseComponent };
function ThfAvatarBaseComponent_tsickle_Closure_declarations() {
    /** @type {?} */
    ThfAvatarBaseComponent.propDecorators;
    /**
     * Fonte da imagem que pode ser um caminho local: ```'./assets/images/logo-black-small.png'```
     * ou servidor externo: ```https://thf.totvs.com.br/assets/images/logo-black-small.png```
     * @type {?}
     */
    ThfAvatarBaseComponent.prototype.src;
    /**
     * Tamanho do componente que aceita os valores: ```xs``` (24x24) ,```sm``` (32x32),```md```(64x64),```lg```(96x96)
     * ou ```xl```(144x144), O valor padrão é ```md```.
     * @type {?}
     */
    ThfAvatarBaseComponent.prototype.size;
}
