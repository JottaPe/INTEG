import { OnInit, EventEmitter, ElementRef } from '@angular/core';
import { ControlValueAccessor, NgControl } from '@angular/forms';
import { ThfRadioGroupOption } from './thf-radio-group.interface';
/**
 * @description
 *
 * O componente thf-radio-group deve ser utilizado para disponibilizar multiplas
 * opções ao usuário, permitindo a ele que selecione apenas uma delas.
 */
export declare class ThfRadioGroupBaseComponent implements OnInit, ControlValueAccessor {
    /** Nome dos radios */
    name: string;
    /** Indica que o campo será desabilitado */
    disabled?: boolean;
    setDisabled: string;
    /** Indica que o campo será obrigatório */
    required?: boolean;
    setRequired: string;
    /** Lista de opções que serão exibidas
     * Nesta propriedade deve ser definido um array de objetos que implementam a interface ThfRadioGroupOption */
    options: Array<ThfRadioGroupOption>;
    /** Evento ao alterar valor do campo */
    change?: EventEmitter<{}>;
    inputEl: ElementRef;
    private el;
    private ngControl;
    selectCheckedValue: string;
    propagateChange: any;
    constructor(el: ElementRef, ngControl?: NgControl);
    ngOnInit(): void;
    keyup(): void;
    eventClick(value: any, disabled: any): void;
    changeValue(value: any): void;
    writeValue(value: any): void;
    registerOnChange(fn: any): void;
    registerOnTouched(): void;
}
