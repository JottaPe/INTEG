import { EventEmitter, ElementRef, OnInit } from '@angular/core';
import { ControlValueAccessor, NgControl } from '@angular/forms';
import { ThfCheckboxGroupOption } from './thf-checkbox-group.interface';
/**
 * @description
 *
 * O componente thf-checkbox-group deve ser utilizado para disponibilizar múltiplas
 * opções ao usuário, permitindo a ele que selecione uma ou múltiplas delas.
 */
export declare class ThfCheckboxGroupBaseComponent implements OnInit, ControlValueAccessor {
    /** Nome dos checkboxes */
    name: string;
    /** Indica que o campo será obrigatório */
    required?: boolean;
    setRequired: string;
    /** Lista de opções que serão exibidas
       * Nesta propriedade deve ser definido um array de objetos que implementam a interface ThfCheckboxGroupOption */
    options: Array<ThfCheckboxGroupOption>;
    /** Evento ao alterar valor do campo */
    change?: EventEmitter<{}>;
    inputEl: ElementRef;
    private el;
    private ngControl;
    selectCheckedValue: any;
    propagateChange: (value: any) => {};
    constructor(el: ElementRef, ngControl?: NgControl);
    ngOnInit(): void;
    eventClick(name: any, value: any): void;
    changeValue(value: any): void;
    writeValue(value: any): void;
    registerOnChange(fn: any): void;
    registerOnTouched(): void;
}
