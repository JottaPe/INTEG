import { EventEmitter } from '@angular/core';
/**
 * @description
 *
 * Cada thf-tab gera uma aba com o conteúdo HTML inserido dentro dele.
 */
export declare class ThfTabBaseComponent {
    /** Label que aparecerá na aba */
    label: string;
    /** Para a aba parecer ativa e mostrar o conteúdo quando iniciar */
    active?: boolean;
    setActive: string;
    /** Esconder a aba */
    hide?: boolean;
    setHide: string;
    /** Desabilitar a aba */
    disabled?: boolean;
    setDisabled: string;
    /** ID da aba */
    id?: string;
    /** Método disparado ao clicar na aba */
    click: EventEmitter<{}>;
}
